# 커넥션 풀 구현해보기
[커넥션 풀](https://github.com/quddaz/java-janggi/tree/step2-cp/src/main/java/config/qudaCP), [Transactional](https://github.com/quddaz/java-janggi/tree/step2-cp/src/main/java/config/transactional)

### 오버 엔지니어링은 학습을 낳는다.

## 구현 시 AI 이용 규칙
- 코드를 짜게 하지말고 질문만 하게 한다.
- 개념을 묻지말고 자신의 개념만 검증 한다.

## 커넥션 관리를 어떻게 해야할까???

### 1. 커넥션를 하나 만들고 close를 사용하지 않고 계속 유지한다.
- 장점 :
    - 하나의 커넥션을 재사용하기에 커넥션 오버헤드 감소
- 단점 :
    - 동시성 처리 불가
    - null과 비슷한 맥락 개발자가 플러시 및 초기화를 신경써야함

### 2. 커넥션를 매번 만들어서 사용한다.
- 장점 :
    - 상태 완전 격리 (매번 새 커넥션)
    - 안정성 높음
    - 구현 단순
- 단점 :
    - DB 연결 비용 매우 큼 (TCP + 인증)

## 간이 커넥션 풀을 만들자....

## 내가 이해한 커넥션 풀이란?
- 1. 사용자의 요청에 사용 가능한 커넥션을 반환한다.
- 2. Close 시 프록시로 가로채 논리적 종료만 한다.
- 3. 반환된 커넥션을 초기 상태로 설정을 바꾸고 다시 커넥션 풀에 넣는다
- 4. 커넥션이 부족하면 확장한다. 개발자가 정의한 맥스 사이즈까지...
- 5. 커넥션 반환 타임 아웃을 유지해야 한다.

심화
- 6. 커넥션에 대한 시간 관리를 해야한다. (오래 사용되지 않으면 삭제)


## 커넥션 풀에 필요한 것은 무엇일까?
- close를 가로채는 프록시가 필요 (아마 프록시 패턴일 것 같다.)
- 커넥션 풀에서 점유와 대기 필요 (찾아보니 Blockingqueue 라는 것이 있다고 한다.)
- 커넥션 반환 시 초기화 필요 (오토커밋, 리드온리, 격리 수준)
- 타임 아웃 관련 로직 필요 (블록킹큐는 타임아웃 기반 반환 기능이 있다.)
- 최대 커넥션 갯수 필요 (AtomicInteger로 스레드 세이프티)

## 스프링에서 트랜잭셔널 구현 방식
1. 스프링에서 커넥션 관리 구조
```
@Transactional을 사용하면 내부적으로 이렇게 동작한다:

메서드 진입 시
→ 트랜잭션 시작
→ 커넥션을 하나 가져옴 (DataSource에서)
그 커넥션을 ThreadLocal에 바인딩
같은 스레드 안에서는
→ Repository / DAO에서 같은 커넥션을 재사용
메서드 종료 시
→ commit or rollback
→ 커넥션을 풀로 반환 (close 호출됨)
```
즉 구현해야할 것은
- @Transactional 어노테이션
- 커넥션 ThreadLocal 바인딩
- Repository 내에서 스레드 로컬에서 커넥션을 꺼내 사용

[스레드로컬-망나니 개발자](https://mangkyu.tistory.com/333)

[다이나믹 프록시](https://inpa.tistory.com/entry/JAVA-%E2%98%95-%EB%88%84%EA%B5%AC%EB%82%98-%EC%89%BD%EA%B2%8C-%EB%B0%B0%EC%9A%B0%EB%8A%94-Dynamic-Proxy-%EB%8B%A4%EB%A3%A8%EA%B8%B0)
